class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
                
        count = ''
        max_len = 0
        for char in s:
            if char in count:
                chars = chars[chars.index(char) + 1:]
            else:
                
                
                            
            return len
            